@typeparam T



<DataGridColumn TItem="T" Caption="@Field.Caption" Editable="false"
                DisplayOrder="@Order" Width="@Field.Width"
                SortDirection="@Field.Sort">
	<DisplayTemplate>
		<Tooltip Text="@(Field.ToolTip?.Invoke(context) ?? Field.Name)" Placement="TooltipPlacement.TopStart" ShowArrow="false">
			<Icon Name="@((Blazorise.IconName) Field.Getter(context))">
			</Icon>
		</Tooltip>
	</DisplayTemplate>
</DataGridColumn>
@code {
	[Parameter]
	public VisibleField<T> Field { get; set; }
	[Parameter]
	public int Order { get; set; }
}
